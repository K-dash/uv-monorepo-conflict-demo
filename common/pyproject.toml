[project]
name = "common"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests==2.32.1",
    "boto3==1.36.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
